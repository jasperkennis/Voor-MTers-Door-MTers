development:
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: voor_mters_door_mters
  pool: 5
  username: user
  password: password
  host: localhost
  
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: voor_mters_door_mters_test # Note this should NOT be the same database as your developemnt or production db
  pool: 5
  username: user # I'ts fine to use the same user for testing as for development
  password: password
  host: localhost
